# 3D Portfolio

A lightweight, dependency-free 3D portfolio landing page. It uses plain HTML, CSS, and JavaScript, so `npm install` can complete without pulling packages from the registry.

## Getting started

Install dependencies and generate the lockfile:

```bash
npm install
```

Run the local development server:

```bash
npm run dev
```

Validate the static files:

```bash
npm run build
```
